diff --git a/pkg/core/track.go b/pkg/core/track.go
--- a/pkg/core/track.go
+++ b/pkg/core/track.go
@@ -86,7 +86,7 @@ func NewSender(media *Media, codec *Codec) *Sender {
 		bufSize = 64
 		}
 	} else {
-		bufSize = 128
+		bufSize = 5
 	}

 	buf := make(chan *Packet, bufSize)
